

export const leerPedidosAPI = () => {
    try {
        // const respuesta = await fetch(api_pedidos);
        // const listaPedidos = await respuesta.json;
        // return listaPedidos;
    } catch (error) {
        console.log(error);
    }
}